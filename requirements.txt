# AB 实验分流算法 - 核心依赖
# 基础环境（所有脚本都需要）
mmh3>=4.0
numpy>=1.20
scipy>=1.7

# 数据驱动验证（仅 did_cuped_kaggle.py / full_scale_validation.py 等需要）
pandas>=1.3
kagglehub>=0.1

# 可视化（仅 mab_vs_ab.py 需要画图）
# matplotlib>=3.5

# 测试（开发时需要）
pytest>=7.0
