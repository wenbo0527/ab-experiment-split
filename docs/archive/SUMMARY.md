# AB 实验分流方案总结（5000 用户 / 10 组 / 偏差 < 1%）

## 一、问题定义

- **业务场景**：5000 用户分 10 组，要求每组人数偏差 < 1%
- **现实痛点**：50:50 配置实测跑出 5% 偏差，甚至单日 29:71
- **核心约束**：同一用户实验期内必须在同一组（一致性）

## 二、四道核心坎

| 阶段 | 问题 | 解法 |
|---|---|---|
| 1. 分桶 | 哈希碰撞导致桶间人数不均 | MurmurHash3 + 1000 桶 |
| 2. 校准 | 1000 桶仍兜不住 1% 偏差（理论下界 13.4%） | 蛇形分配（S-shape） |
| 3. 验证 | 人数匀 ≠ 分流对 | Hash_diff + SRM + AA 三层验证 |
| 4. 检测能力 | 偏差控制 ≠ 能检出效果 | MDE 评估样本量 |

## 三、五套方案实验数据汇总

### 实验配置
- 用户量：5000
- 桶数：1000
- 组数：10
- 抽样次数：每方案 50-100 次
- 评估指标：平均最大偏差、< 1% 通过率、Hash_diff、SRM 通过率

### 实验结果

| 方案 | 平均偏差 | P95 偏差 | < 1% 通过率 | SRM 通过率 | 一致性 | 工程成本 |
|---|---|---|---|---|---|---|
| **批量蛇形分配**（ab_split_validator） | **0.51%** | 0.80% | **96%** | 100% | ✓ | 低 |
| 实时无状态 hash（R0） | 7.98% | 12.40% | 0% | 90% | ✓ | 极低 |
| 实时两次 hash 异或 | 7.82% | 11.61% | 0% | 93% | ✓ | 低 |
| 实时多 salt 众数投票 | 7.98% | 11.62% | 0% | 97% | ✓ | 中 |
| 实时桶级微调（S2） | 5.99% | 8.51% | 0% | 100% | ✓ | 中 |
| 实时多映射切换（S3） | 0.51% | 0.80% | 98% | 100% | ✗ | 中 |
| 预分桶查表（生产推荐） | 0.51% | 0.80% | 96% | 100% | ✓ | 低 |

### 多层正交实验（流量复用）

> 数据来源：`orthogonal_layers.py`（5000 用户 × 50 次抽样 × 3 层每层 2 组）

| 指标 | 实测值 | 判定 |
|---|---|---|
| 正交通过率（p > 0.05） | **100%** | ✓ 完美正交 |
| 卡方 p-value 中位数 | 0.48 | 远大于 0.05 |
| 列联表最大偏差 | 1.13% | 与组数 2 的泊松波动一致 |
| 三层 8 组合分布平衡率 | 0.89 | 接近理想 1.0 |

**多层正交的单层偏差**（实时，每层 2 组）：

| 层 | 平均偏差 | P95 | < 1% 通过率 |
|---|---|---|---|
| L1 推荐 | 1.24% | 2.89% | 52% |
| L2 搜索 | 0.89% | 2.11% | 60% |
| L3 UI | 1.35% | 2.55% | 34% |

**多层正交的价值**：100% 流量可被 3 个实验同时使用，层间完全独立（卡方检验 100% 通过）。单层偏差仍是 √n 下界——多层正交**解决流量复用，不解决单层均匀性**。

### 样本量扫描（实时分流偏差下限）

| 用户量 | 平均偏差 | < 1% 通过率 | 评价 |
|---|---|---|---|
| 500 | 24.64% | 0% | 灾难 |
| 5000 | 7.98% | 0% | 当前水平 |
| 50000 | 2.37% | 0% | 接近 |
| 100000 | 1.67% | 10% | 刚刚摸到 1% 边缘 |
| 理论 50 万 | 1.34% | — | 3σ 理论值 |

## 四、关键发现

### 发现 1：单层实时分流存在 √n 数学下界

5000 用户 / 1000 桶 / 10 组，理论下界：
- 每桶期望 5 人，桶间 σ = √5 ≈ 2.24
- 每组聚合 100 桶，组间 σ = √100 × 2.24 = 22.4
- 3σ 相对偏差 = 3 × 22.4 / 500 = **13.4%**

实测平均偏差 7.98%，与理论吻合。**任何 O(1) 的局部优化（加桶数、两次 hash、多 salt）都改不了 √n 渐近线**。

### 发现 2：批量蛇形分配是真解

蛇形分配不是 O(1) 优化，而是**全局贪心均衡算法**：
1. 全量哈希分 1000 桶
2. 按桶人数降序排列
3. 第 1 大桶→组 0，第 2 大桶→组 1……第 G 大桶→组 G-1
4. 第 G+1 大桶→组 G-1，第 G+2 大桶→组 G-2……交替反向

实测 96% 通过率，平均偏差 0.51%。**这是 5000 量级唯一能稳定压到 1% 的算法**。

### 发现 3：S3 多映射切换"看似达标实则违规"

S3 平均偏差 0.51%、98% 通过率，但**切换映射时破坏一致性**——同一用户在不同时间窗口可能被分到不同组。在生产中等同于实验数据污染。

### 发现 4：流量放大 100 倍才能纯实时压到 1%

要单层实时分流压到 1%，需要把流量从 5000 提升到 50 万（√n 改善）。这不是算法问题，是统计物理下界。

### 发现 5：多层正交通过卡方独立性检验

实验验证 3 层（推荐/搜索/UI 各 2 组）的正交性，**100% 通过卡方独立性检验**（p > 0.05），列联表最大偏差仅 1.13%。说明：
- 每层独立 salt 哈希能保证层间完全独立
- 100% 流量可被多个实验同时使用
- 适用于"流量不够分"的工业级场景

但要注意：**多层正交不解决单层偏差问题**。每层仍然是 √n 下界（5000 用户单层偏差 1-2%）。多层正交的价值在"流量复用"，不在"偏差控制"。

## 五、推荐方案

### 推荐方案：预分桶 + 静态查表（生产级）

**步骤**：
1. 实验启动前用 `ab_split_validator.assign_groups()` 一次性算出所有用户的分组
2. 生成 `user_id → group_id` 映射表，部署到 Redis / ConfigMap
3. 运行时 `O(1)` 查表：`group = redis.get(f"exp:{exp_id}:{user_id}")`
4. 新用户首次进入实验时，落入预分桶表

**优势**：
- 偏差 < 1%（继承蛇形分配）
- 100% 一致性（同一用户始终同组）
- 运行时无哈希计算，性能极优
- 字节、阿里、美团等生产平台标准做法

**适用场景**：
- 用户量 5000 - 100 万
- 实验周期 1-60 天
- 对一致性敏感的所有场景

### 不推荐方案：实时 + 中间校验 + 动态再均衡

**问题**：
- 算法复杂，状态管理成本高
- 一致性破坏风险（S3）
- 校准效果有限（S2 仅降到 6%）
- 收益不如直接做预分桶

### 进阶方案：多层正交（适用于多实验并发）

**适用场景**：
- 推荐、搜索、UI、广告等多个团队同时要做实验
- 单个实验独占 100% 流量，团队间没有多余流量分配
- 需要让多个实验同时复用 100% 流量

**实现**：
```python
# 每层用独立 salt，保证层间独立
group_l1 = hash(uid + "rec_salt") % 1000 % 2
group_l2 = hash(uid + "sch_salt") % 1000 % 2
group_l3 = hash(uid + "ui_salt")  % 1000 % 2
```

**数学保证**：
- N(A∩C) = N(A) × N(C) / N_total
- 卡方独立性检验 100% 通过
- 三层 8 种组合的分布平衡率 0.89

**注意事项**：
- 多层正交解决流量复用，**不解决单层偏差**
- 每层仍然需要蛇形分配或预分桶查表来保证偏差 < 1%

## 六、文件清单

| 文件 | 作用 |
|---|---|
| `ab_split_validator.py` | 主算法：蛇形分配 + 三层验证 + MDE |
| `monte_carlo_100.py` | 100 次重复抽样稳定性测试 |
| `streaming_vs_batch.py` | 批量 vs 实时分流偏差对比 |
| `realtime_remedy.py` | 实时分流单层优化方案对比（单hash/两次hash/多salt） |
| `realtime_breakthrough.py` | 实时分流偏差下界扫描 + 理论推导 |
| `realtime_adaptive.py` | 中间校验 + 动态再均衡方案 |
| `orthogonal_layers.py` | 多层正交实验 + 流量复用验证 |

## 七、核心算法代码（可直接复用）

### 7.1 批量蛇形分配（单实验最优）

```python
import mmh3

def assign_groups(user_ids, num_buckets=1000, num_groups=10, salt="exp_001"):
    """蛇形分配：5000 用户分 10 组，偏差 < 1%"""
    # Step 1: 哈希分桶
    buckets = {i: [] for i in range(num_buckets)}
    for uid in user_ids:
        bid = mmh3.hash(f"{uid}_{salt}", signed=False) % num_buckets
        buckets[bid].append(uid)

    # Step 2: 按桶人数降序
    sorted_buckets = sorted(buckets.items(), key=lambda x: len(x[1]), reverse=True)

    # Step 3: 蛇形分配
    groups = {i: [] for i in range(num_groups)}
    for idx, (_, users) in enumerate(sorted_buckets):
        cycle = idx // num_groups
        pos = idx % num_groups
        gid = pos if cycle % 2 == 0 else (num_groups - 1 - pos)
        groups[gid].extend(users)

    return groups
```

### 7.2 多层正交分流器（多实验并发）

```python
import mmh3

def route_layer(user_id, layer_name, num_buckets=1000, num_groups=2):
    """
    单层路由：每层用独立 salt
    跨层正交：N(A∩C) = N(A) × N(C) / N_total
    """
    salt = f"exp_{layer_name}"  # 层间用独立 salt
    bucket = mmh3.hash(f"{user_id}_{salt}", signed=False) % num_buckets
    return bucket % num_groups


# 多实验并发：3 个实验共用 100% 流量
group_l1 = route_layer(user_id, "recommend")  # 推荐实验 A/B
group_l2 = route_layer(user_id, "search")     # 搜索实验 A/B
group_l3 = route_layer(user_id, "ui")         # UI 实验 A/B/C
```

## 八、一句话总结

**5000 用户分 10 组压到 < 1% 偏差的唯一稳定方案是：批量蛇形分配 → 预生成分组表 → 运行时 O(1) 查表**。实时分流在 5000 量级下存在 √n 数学下界，任何 O(1) 优化都改不了这个物理极限。多层正交是字节 DataTester 的流量复用方案，能让多个实验同时复用 100% 流量，但**不解决单层偏差问题**——每层仍然需要蛇形分配或预分桶查表来保证偏差 < 1%。